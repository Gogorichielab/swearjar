{
  "name": "swearjar-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Azure Functions backend for SwearJar",
  "main": "src/index.js",
  "scripts": {
    "start": "func start",
    "lint": "node --check src/index.js",
    "test": "jest --testPathPatterns=src/",
    "test:coverage": "jest --testPathPatterns=src/ --coverage"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@azure/data-tables": "^13.3.0",
    "@azure/functions": "^4.5.0"
  },
  "devDependencies": {
    "jest": "^30.3.0"
  }
}
